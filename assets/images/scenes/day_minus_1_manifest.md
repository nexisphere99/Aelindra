# Day -1   Scene Images

Same `<<sceneImage>>` native macro as Day -2 (see
`day_minus_2_manifest.md`)   fails silently if missing.

No Day -1 art currently qualifies as a scene image rather than a location  
everything wired so far (`moonspire_tower.png`, `moonspire_ritual_chamber.png`,
`moonspire_apothecary.png`) is a reusable room, so it stays in
`assets/images/locations/day_minus_1_manifest.md`. This file exists so the
per-day structure is consistent and ready if a Day -1 narrative-beat image
gets added later (e.g. Ash-specific art, an abstract/emotional illustration
tied to one specific passage).
